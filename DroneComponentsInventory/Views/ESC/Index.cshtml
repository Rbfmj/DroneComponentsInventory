@model IEnumerable<DroneComponentsInventory.Models.ESCComponent>
@using System.Linq

@{
    /* This view is rendered standalone (no shared layout) */
    Layout = null;
}

<!DOCTYPE html>
<html lang="lv">
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Elektroniskie ātruma regulatori</title>

    <link rel="stylesheet" href="/css/main.css" />
    <link rel="stylesheet" href="/css/esc.css" />
</head>
<body class="is-preload">

    <partial name="_IndexQuickNav" />

    <div class="esc-view">

        <div class="wrapper style1 align-center">
            <div class="inner">
                <h2>Elektroniskie ātruma regulatori</h2>
                <p>Izvēlieties Elektroniskie ātruma regulatoru, lai skatītu specifikācijas.</p>

                <p>
                    <a asp-action="Create" class="button small">Pievienot jaunu Elektroniskie ātruma regulatoru</a>
                </p>
            </div>
        </div>

        <div class="wrapper">
            <div class="inner">
                <div class="esc-grid" id="escGrid">
                    @foreach (var item in Model ?? Enumerable.Empty<DroneComponentsInventory.Models.ESCComponent>())
                    {
                        <article class="esc-tile" data-modal-id="emodal-@item.EscId">
                            <div>
                                <span class="title">@item.Manufacturer</span>
                                <span class="muted">@item.Model</span>
                            </div>
                        </article>
                    }
                </div>
            </div>
        </div>

        @foreach (var item in Model ?? Enumerable.Empty<DroneComponentsInventory.Models.ESCComponent>())
        {
            <div id="emodal-@item.EscId" class="esc-modal" style="display:none;" aria-hidden="true">
                <div class="panel">
                    <button type="button" class="button small close-modal" style="position:absolute; right:12px; top:12px;">×</button>

                    <h2>@item.Manufacturer @item.Model</h2>

                    <dl class="esc-meta">
                        <dt>Tips</dt>
                        <dd>@item.EscType</dd>

                        <dt>Strāvas patēriņš (A)</dt>
                        <dd>@(item.ContinuousCurrentA?.ToString() + " A" ?? "")</dd>

                        <dt>Ieejas spriegums (S)</dt>
                        <dd>@(item.VoltageInputS?.ToString() + "S" ?? "")</dd>

                        <dt>Montāžas raksts (mm)</dt>
                        <dd>@(item.MountPatternMm?.ToString() + " mm" ?? "")</dd>

                        <dt>Atbalstītie protokoli</dt>
                        <dd>@item.SupportedProtocols</dd>

                        <dt>Svars (g)</dt>
                        <dd>@(item.WeightG?.ToString() + " g" ?? "")</dd>

                        <dt>Cena</dt>
                        <dd>@item.Price</dd>
                    </dl>

                    <div style="margin-top: 16px; display:flex; gap:12px; justify-content:flex-end;">
                        <a class="button small" asp-action="Edit" asp-route-id="@item.EscId">Rediģēt</a>
                        <a class="button small" asp-action="Details" asp-route-id="@item.EscId">Pārskats</a>
                        <a class="button small danger" asp-action="Delete" asp-route-id="@item.EscId">Dzēst</a>
                        <button type="button" class="button small close-modal">Aizvērt</button>
                    </div>
                </div>
            </div>
        }

    </div>

    <script src="/js/esc-modals.js"></script>

</body>
</html>
