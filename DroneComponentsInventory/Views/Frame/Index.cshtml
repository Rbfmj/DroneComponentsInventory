@model IEnumerable<DroneComponentsInventory.Models.FrameComponent>
@using System.Linq

@{
    /* Šī skats tiek rādīts patstāvīgi (nav kopīgas noformējuma) */
    Layout = null;
}

<!DOCTYPE html>
<html lang="lv">
<head>
    <!-- Pamata atsaucīgā meta -->
    <meta name="viewport" content="width=device-width" />

    <title>Rāmji</title>

    <!-- Tīmekļa vietnes kodols stilus -->
    <link rel="stylesheet" href="/css/main.css" />
    <link rel="stylesheet" href="/css/frames.css" />
</head>

<body class="is-preload">

    <partial name="_IndexQuickNav" />

    <div class="frames-view">

        <div class="wrapper style1 align-center">
            <div class="inner">
                <h2>Rāmji</h2>

                <p>Izvēlieties rāmi, lai skatītu tehnisko specifikāciju.</p>

                <p>
                    <a asp-action="Create" class="button small">Pievienot jaunu rāmi</a>
                </p>
            </div>
        </div>

        <div class="wrapper">
            <div class="inner">

                <div class="frames-grid" id="framesGrid">

                    @foreach (var item in Model ?? Enumerable.Empty<DroneComponentsInventory.Models.FrameComponent>())
                    {
                        <article class="frames-tile" data-modal-id="fmodal-@item.FrameId">
                            <div>
                                <span class="title">@item.Manufacturer</span>
                                <span class="muted">@item.Model</span>
                            </div>
                        </article>
                    }

                </div>

            </div>
        </div>

        @foreach (var item in Model ?? Enumerable.Empty<DroneComponentsInventory.Models.FrameComponent>())
        {
            <div id="fmodal-@item.FrameId" class="frames-modal" style="display:none;" aria-hidden="true">
                <div class="panel">

                    <button type="button" class="button small close-modal" style="position:absolute; right:12px; top:12px;">×</button>

                    <h2>@item.Manufacturer @item.Model</h2>

                    <dl class="frames-meta">
                        <dt>Riteņu bāze</dt>
                        <dd>@(item.WheelbaseMm?.ToString() + " mm" ?? "")</dd>

                        <dt>Maks. propellera izmērs</dt>
                        <dd>@(item.MaxPropInch?.ToString() + "\"" ?? "")</dd>

                        <dt>Ģeometrija</dt>
                        <dd>@item.Geometry</dd>

                        <dt>Materiāls</dt>
                        <dd>@item.Material</dd>

                        <dt>Svars</dt>
                        <dd>@(item.WeightG?.ToString() + " g" ?? "")</dd>

                        <dt>Roku biezums</dt>
                        <dd>@(item.ArmThicknessMm?.ToString() + " mm" ?? "")</dd>

                        <dt>Motora stiprinājuma raksts</dt>
                        <dd>@item.MotorMountPattern</dd>

                        <dt>FC stiprinājuma raksts</dt>
                        <dd>@item.FcMountPattern</dd>

                        <dt>Maks. dzesēšanas augstums</dt>
                        <dd>@(item.MaxStackHeightMm?.ToString() + " mm" ?? "")</dd>

                        <dt>Cena</dt>
                        <dd>@(item.Price.HasValue? item.Price.Value.ToString("0.00") : "")</dd>
                    </dl>

                    <div style="margin-top:16px; display:flex; gap:12px; justify-content:flex-end;">
                        <a class="button small" asp-action="Edit" asp-route-id="@item.FrameId">Rediģēt</a>
                        <a class="button small" asp-action="Details" asp-route-id="@item.FrameId">Detalizēti</a>
                        <a class="button small danger" asp-action="Delete" asp-route-id="@item.FrameId">Dzēst</a>
                        <button type="button" class="button small close-modal">Aizvērt</button>
                    </div>

                </div>
            </div>
        }

    </div>

    <script src="/js/frames-modals.js"></script>

</body>
</html>