@model IEnumerable<DroneComponentsInventory.Models.FCComponent>
@using System.Linq

@{
    /* This view is rendered standalone (no shared layout) */
    Layout = null;
}

<!DOCTYPE html>
<html lang="lv">
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Lidojuma kontrolieri</title>

    <link rel="stylesheet" href="/css/main.css" />
    <link rel="stylesheet" href="/css/fc.css" />

    <noscript>
        <link rel="stylesheet" href="/css/noscript.css" />
    </noscript>
</head>

<body class="is-preload">

    <partial name="_IndexQuickNav" />

    <div class="fc-view">

        <div class="wrapper style1 align-center">
            <div class="inner">
                <h2>Lidojuma kontrolieri</h2>
                <p>Izvēlieties lidojuma kontrolieri, lai skatītu tehnisko specifikāciju.</p>
                <p><a asp-action="Create" class="button small">Pievienot jaunu lidojuma kontrolier</a></p>
            </div>
        </div>

        <div class="wrapper">
            <div class="inner">
                <div class="fc-grid" id="fcGrid">
                    @foreach (var item in Model ?? Enumerable.Empty<DroneComponentsInventory.Models.FCComponent>())
                    {
                        <article class="fc-tile" data-modal-id="fmodal-@item.FcId">
                            <div>
                                <span class="title">@item.Manufacturer</span>
                                <span class="muted">@item.Model</span>
                            </div>
                        </article>
                    }
                </div>
            </div>
        </div>

        @foreach (var item in Model ?? Enumerable.Empty<DroneComponentsInventory.Models.FCComponent>())
        {
            <div id="fmodal-@item.FcId" class="fc-modal" style="display:none;" aria-hidden="true">
                <div class="panel">
                    <button type="button" class="button small close-modal" style="position:absolute; right:12px; top:12px;">×</button>
                    <h2>@item.Manufacturer @item.Model</h2>

                    <dl class="fc-meta">
                        <dt>Procesors</dt>
                        <dd>@item.McuProcessor</dd>

                        <dt>IMU / Žiroskops</dt>
                        <dd>@item.ImuGyro</dd>

                        <dt>Montāžas raksts</dt>
                        <dd>@(item.MountPatternMm?.ToString() + " mm" ?? "")</dd>

                        <dt>Sprieguma ieeja</dt>
                        <dd>@item.VoltageInputS</dd>

                        <dt>Programmatūra</dt>
                        <dd>@item.FirmwareSupport</dd>

                        <dt>Svars</dt>
                        <dd>@(item.WeightG?.ToString() + " g" ?? "")</dd>

                        <dt>Cena</dt>
                        <dd>@item.Price</dd>
                    </dl>

                    <div style="margin-top:16px; display:flex; gap:12px; justify-content:flex-end;">
                        <a class="button small" asp-action="Edit" asp-route-id="@item.FcId">Rediģēt</a>
                        <a class="button small" asp-action="Details" asp-route-id="@item.FcId">Detalizēti</a>
                        <a class="button small danger" asp-action="Delete" asp-route-id="@item.FcId">Dzēst</a>
                        <button type="button" class="button small close-modal">Aizvērt</button>
                    </div>
                </div>
            </div>
        }

    </div>

    <script src="/js/fc-modals.js"></script>

</body>
</html>
